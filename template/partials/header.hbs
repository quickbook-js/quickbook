<header class="site-header">
  <div class="header-container">
    <span class="badge">Information Bulletin</span>
    <h1>{{#if pageTitle}}{{pageTitle}}{{else}}Project Documentation{{/if}}</h1>
    {{#if pageDesc}}<p class="header-lead">{{pageDesc}}</p>{{/if}}
    {{#if date}}<time class="post-date">📅 {{date}}</time>{{/if}}
  </div>
</header>
